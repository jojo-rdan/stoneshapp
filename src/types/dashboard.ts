export type DashboardStat = {
  id: string;
  label: string;
  value: string;
  hint: string;
};

export type QuickNote = {
  id: string;
  title: string;
  body: string;
};

export type DashboardTip = {
  id: string;
  title: string;
  body: string;
};
